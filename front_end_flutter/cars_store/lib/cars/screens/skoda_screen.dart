

import 'package:flutter/material.dart';

class SkodaScreen extends StatelessWidget {
  const SkodaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}